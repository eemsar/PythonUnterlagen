#In Python müssen allen Variablen bei der Erstellung eine Wertzuweisung erfolgen, ansonsten wird die Variable verworfen.
#Datentypen müssen bei der Initialisierung nicht angegeben werden.
#Variablennamen die aus mehreren Namen bestehen nutzen die Camelcase (nameDerVariable), Pascalcase (NameDerVariable) oder/und die Snakecase (name_der_variable)


#Zahlen
intzahl = 20
gleitkommazahl = 2.22
print(str(intzahl) + " & " + str(gleitkommazahl))

 
#Boolean 
boolTrue = True
boolFalse = False
print('\n' + str(boolTrue) + " & " + str(boolFalse))


#Strings
stringValue = "\nHello World"
print(stringValue)